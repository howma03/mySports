import {ShowOneDirective} from "./show-one.directive";

describe('ShowOneDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOneDirective();
    expect(directive).toBeTruthy();
  });
});
